
/*
function currentBook (){
    for(let i=0;i<books.length;i++) 
      if (books[i].loc == window.location.href)
      return books[i];
  }

  var read = document.querySelector("readlist")
  read.addEventListener('click' , () => {
    for(let i=0;i<UsersArray.table.length;i++)
      if((UsersArray.table[i].username.localeCompare(currentUser))==0)
        if((UsersArray.table[i].password.localeCompare(currentPass))==0)
            if (!UsersArray.table[i].wantRead.includes(currentBook) )
              app[0].table[i].wantRead.push(books[i]);  
    
  })
  */